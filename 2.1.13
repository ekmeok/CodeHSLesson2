function start() {
    while (frontIsClear()) {
        checkSquare();
        move();
    }
    checkSquare();
}

function checkSquare() {
    if (colorIs(Color.purple)) {
        if (ballsPresent()) {
            takeBall();
            paint(Color.orange);
        }
    }
}
